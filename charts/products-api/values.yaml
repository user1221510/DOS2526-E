app:
  replicaCount: 1
  image:
    repository: dos2526-api
    tag: "dev-francisco-2026-01-19-0007"
    pullPolicy: IfNotPresent
  service:
    # ALTERADO: NodePort permite acesso externo (http://localhost:30050)
    type: NodePort 
    port: 80
    targetPort: 8080
    # Porta padrão (será alterada pelo Jenkins para 30050 ou 30055)
    nodePort: 30050
  env:
    environment: "Development"

database:
  enabled: true
  image:
    repository: mcr.microsoft.com/mssql/server
    tag: "latest"
  password: "GrupoE2526!" 
  service:
    port: 1433
  persistence:
    enabled: true
    size: 2Gi